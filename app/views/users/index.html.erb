

<h2>Welcome To The HomePage!    </h2>

<%= link_to "Menu Page ", menus_path(@menu) %>

<p>Listing Users!</p>


<h3>Users</h3>

<ul>
  <% @users.each do |user| %>
    <li>User ID : 
      <%= user.id %>
        User Name : 
        <!-- <%= user.username %> -->
        <!-- <a href="<%= user_path(user) %>">
            <%= user.username %></a> -->
            <%= link_to user.username, user %>

      </li>
      
  <% end %>
</ul>

<%= link_to "New User", new_user_path %>
